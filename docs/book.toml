[book]
authors = ["Frank Bardon"]
language = "en"
src = "src"
title = "Aperture Documentation"
description = "Fine-grained access-control engine for the frankbardon/* family"

[build]
build-dir = "book"

[output.html]
# Off deliberately: this is a technical book full of literal shell text, and
# smart punctuation rewrote every CLI long flag into an en dash — the generated
# reference rendered `--field` as `–field`, which is not a flag that exists.
smart-punctuation = false
default-theme = "navy"
preferred-dark-theme = "navy"
git-repository-url = "https://github.com/frankbardon/aperture"
edit-url-template = "https://github.com/frankbardon/aperture/edit/main/docs/src/{path}"
no-section-label = false
additional-js = ["mermaid.min.js", "mermaid-init.js"]

[output.html.search]
enable = true
limit-results = 30
boost-hierarchy = 2
